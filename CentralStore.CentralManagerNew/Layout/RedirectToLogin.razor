@inject NavigationManager Navigation

@code {
    override protected void OnInitialized()
    {
        // Redirect to the login page (Keycloak)
        Navigation.NavigateTo("authentication/login", true);
    }
}
